#include"classes/card.bgt"
#include"sound_pool.bgt"

string versao="0.1 beta";

void main()
{
	show_game_window("Patience show - "+versao);
	
	while(true)
	{
		if(key_pressed(KEY_ESCAPE)) exit();
	}
}


