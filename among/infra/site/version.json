{
	"version": "0.22.2",
	"url": "AmongUs.zip",
	"notes": "Every action key in a match can now be remapped in settings — the radar lock (Q) and the radar mode switch were missing; the mode switch now has the M key (Ctrl + Tab still works).",
	"notes_by_language": {
		"pt_BR": "Todas as teclas de ação da partida podem ser trocadas nas configurações — faltavam a de travar o radar (Q) e a de trocar o modo do radar, que agora tem a tecla M (Ctrl + Tab continua valendo).",
		"en_US": "Every action key in a match can now be remapped in settings — the radar lock (Q) and the radar mode switch were missing; the mode switch now has the M key (Ctrl + Tab still works)."
	}
}
